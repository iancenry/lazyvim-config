return {
  -- Disable nvim-treesitter-textobjects (incompatible with current nvim-treesitter)
  -- LazyVim uses mini.ai for text objects instead
  { "nvim-treesitter/nvim-treesitter-textobjects", enabled = false },
}
